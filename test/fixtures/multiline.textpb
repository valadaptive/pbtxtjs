# Multi-line strings test
string_field: "This is a very long string that "
              "spans multiple lines and should "
              "be concatenated together."

string_field: "No space" "between" "strings"

bytes_field: "binary" "data" "concatenated"
